spring:
  application:
    name: ${APP_NAME:test-pfq-authorization-server}
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:test}
      username: user
  output:
    ansi:
      enabled: always