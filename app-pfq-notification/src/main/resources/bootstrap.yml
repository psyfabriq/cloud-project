spring:
  application:
    name: ${APP_NAME:app-pfq-notification}
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:test}
      username: user